<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.mapper.MediaFolderMapper">
    <select id="findAll" resultType="MediaFolder">
        SELECT * FROM `media_folder` ORDER BY id DESC
    </select>
    <select id="findById" resultType="MediaFolder">
        SELECT * FROM `media_folder` WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `media_folder`(name, path) VALUES(#{name}, #{path})
    </insert>
    <delete id="deleteById">
        DELETE FROM `media_folder` WHERE id = #{id}
    </delete>
</mapper>
